import XCTest
@testable import AlgorithmVisualizer

final class AlgorithmVisualizerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AlgorithmVisualizer().text, "Hello, World!")
    }
}
